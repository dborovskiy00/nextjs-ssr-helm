apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nextjs-ssr-helm.fullname" . }}-test-connection"
  labels:
    {{- include "nextjs-ssr-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nextjs-ssr-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
