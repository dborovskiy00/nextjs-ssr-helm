apiVersion: v1
kind: Service
metadata:
  name: {{ include "nextjs-ssr-helm.fullname" . }}
  labels:
    {{- include "nextjs-ssr-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nextjs-ssr-helm.selectorLabels" . | nindent 4 }}
